query Wallets {
  wallets {
    id
    label
    accounts {
      id
      name
      type
      balance
      currency
      lastUpdated
    }
  }
}

mutation CreateWallet($label: String!) {
  createWallet(label: $label) {
    id
    label
  }
}
